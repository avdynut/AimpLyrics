<Window x:Class="AimpLyrics.LyricsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lyrics"
        WindowStyle="ToolWindow"
        Closing="OnClosing"
        SizeToContent="Width" MinWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Row="0" Name="Artist" ToolTip="Artist"/>
            <TextBox Grid.Row="1" Name="Title" ToolTip="Title"/>
            <Button Grid.Column="1" Grid.RowSpan="2" Content="Search" ToolTip="Search lyrics in Google"
                    Click="SearchLyricsInGoogle" IsDefault="True"/>
        </Grid>
        <TextBox Grid.Row="1" Name="Lyrics" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        <WebBrowser Name="Browser" LoadCompleted="OnBrowserLoadCompleted" Visibility="Collapsed"/>
    </Grid>
</Window>
