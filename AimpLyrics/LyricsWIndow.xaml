<Window x:Class="AimpLyrics.LyricsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lyrics"
        WindowStyle="ToolWindow"
        Closing="OnClosing"
        SizeToContent="Width" MinWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Row="0" Name="Artist" ToolTip="Artist"/>
            <TextBox Grid.Row="1" Name="Title" ToolTip="Title"/>
            <Button Grid.Column="1" Grid.RowSpan="2" Content="Search" ToolTip="Search lyrics in Google"
                    Click="OnSearchButtonClick" IsDefault="True"/>
        </Grid>
        <TextBox Grid.Row="1" Name="Lyrics" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="Lyrics Source: "/>
            <TextBlock Name="LyricsSource"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <!--<Button Content="Save To Tag"/>
            <Button Content="Save To File"/>-->
            <!--<CheckBox Content="Save To Tag" IsChecked="True"/>-->
        </StackPanel>
        <StackPanel Grid.Row="4" Name="BrowserPanel" Visibility="Collapsed">
            <StackPanel.Resources>
                <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
            </StackPanel.Resources>
            <CheckBox Name="ShowBrowser" Content="Show Browser"/>
            <WebBrowser Name="Browser" LoadCompleted="OnBrowserLoadCompleted"
                        Visibility="{Binding ElementName=ShowBrowser, Path=IsChecked, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>
    </Grid>
</Window>
